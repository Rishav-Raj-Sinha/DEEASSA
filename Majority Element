class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        n = len(nums)
        major  = n/2
        unique = set()
        for num in nums:
            unique.add(num)
        dict = {}
        for num in unique:
            count = 0
            for number in nums :
                if number == num:
                    count = count+1
            dict[count] = num
        for key in dict:
            if key > major:
                return dict[key]
            
